---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forms
  labels:
    app: forms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: forms
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: forms
      annotations:
        prometheus.io/scrape: 'false'
    spec:
      containers:
        - name: forms
          image: forms
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /api/ping
              port: 8080
              scheme: HTTP
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 1
            initialDelaySeconds: 20
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /api/ping
              port: 8080
              scheme: HTTP
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 1
            initialDelaySeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 50Mi
            limits:
              memory: 50Mi
