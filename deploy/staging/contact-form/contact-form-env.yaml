---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forms
  labels:
    app: forms
spec:
  selector:
    matchLabels:
      app: forms
  template:
    spec:
      containers:
        - name: forms
