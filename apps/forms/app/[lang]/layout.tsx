import RootLayout, { generateStaticParams } from '@fdk-frontend/ui/layout-root';

const FormsLayout = (props: any) => (<RootLayout {...props} />);

export default FormsLayout;
export { generateStaticParams };
